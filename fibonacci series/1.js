var first, second,n;


first = parseInt(prompt("enter the previous first number of n element"));
second = parseInt(prompt("enter the previous number of n element"));

n = (first + second);

alert(n);

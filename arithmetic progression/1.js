var first, diff, Nth,tn;

first = prompt("enter the first number of arithmetic progression");
diff = prompt("enter the difference of arithmetic progression");
Nth = prompt("enter the address of number you want to know");

tn = (first + (Nth-1) * diff);
alert(tn);